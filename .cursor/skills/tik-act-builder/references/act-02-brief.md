# Act 2 Agent — Information moment (echeon)

**Kern:** Kuratierter Feed — **wo** die Persona ihn konsumiert, entscheidet `session_arc` (Tablet, Monitor, Papier, Werkstatt, …).

**Deliverables:**
- `pre_beat_track` 01 (Szene aus session_arc)
- `beat_track` 02 (echeon erklären)
- 2–4 `echeon_feed` headlines
- Splat: `session_arc.splat_prompt_en` verfeinern
- Cues: ambient → Hinweis auf primary_anchor → overlay bei Trigger

**Checklist:**
- [ ] Ort passt zu Persona + Company (nicht automatisch Küche)
- [ ] `trigger_type` / `trigger_target` aus session_arc
